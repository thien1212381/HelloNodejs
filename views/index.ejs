<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>DEMO</title>
  </head>
  <body>
      <h1>DEMO</h1>
      <ul>
        <% list.forEach(function(item){ %>
          <li> <%= (item.id+" - "+item.name) %> <a href="/remove/<%= item.id %>">Remove</a> </li>
        <% }) %>
      </ul>
      <form class="" action="/add" method="post">
        <input type="text" name="username" value="">
        <input type="submit" name="btn" value="OK">
      </form>
  </body>
</html>
